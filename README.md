# tiny-webhook example project

Simple project to test CI/CD with [tiny-webhook](https://github.com/BtheDestroyer/tiny-webhook)

## Building

To build, run `make` or `make all` with g++ installed.

To run, run `make run`

## Expected Output

```
Hello, CI/CD!
I was compiled with commit f8ccf5b!
```
